import { colors } from './colors';
import { fontSizes, fontWeights, lineHeights } from './typography';
import { spacing } from './spacing';

export const theme = {
  colors,
  fontSizes,
  fontWeights,
  lineHeights,
  spacing,
};

export { colors, fontSizes, fontWeights, lineHeights, spacing };
export type { ColorKey } from './colors';
