/**
 * Consistent spacing; touch targets min 44pt (use minTouchTarget).
 */
export const spacing = {
  xxs: 4,
  xs: 8,
  sm: 12,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
  /** Minimum touch target for accessibility (44pt) */
  minTouchTarget: 44,
} as const;
